# Application
APP_ENV=development
APP_DEBUG=true

# PostgreSQL
POSTGRES_USER=epa_user
POSTGRES_PASSWORD=change_me_in_production
POSTGRES_DB=epa_db
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Database URL (constructed from above)
DATABASE_URL=postgresql://epa_user:change_me_in_production@localhost:5432/epa_db

# LLM (not wired yet — placeholder for future use)
# LLM_PROVIDER=
# LLM_API_KEY=
# LLM_MODEL=
